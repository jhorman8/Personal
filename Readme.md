SmartCar API V0.01

The smartCar Api was build in javascipt using azure functions core tools in version 4

you could find the code including the unit test, also you could find a postman collection to check all the enpoints created

Pending tasks

- Add security to protect all the enpoints, we have to options: Add the security using a middleware or just using the keys provided by azure, also we could set OAUTH2.0 with some custom provider like Azure AD
- Add linters to scan the code and detect vulnerabilities like SQL inyection
- Create a CI/CD for the code deployment
- Optimize the code to re-use code.


